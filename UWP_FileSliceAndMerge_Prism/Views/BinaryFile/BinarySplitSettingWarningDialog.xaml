<ContentDialog
    x:Class="UWP_FileSliceAndMerge_Prism.Views.BinarySplitSettingWarningDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_FileSliceAndMerge_Prism.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Invalid Input"
    x:Uid="ContentDialog_InvalidInput"
    PrimaryButtonText="Ok"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid>
        <StackPanel>
            <TextBlock Text="{x:Bind TheMaximumNumberOfSlices}"
                       Style="{StaticResource BodyTextBlockStyle}"/>
            <TextBlock  Style="{StaticResource BodyTextBlockStyle}"
                        Margin="{StaticResource SmallTopMargin}"
                        Text="{x:Bind CurrentSituation}"/>
        </StackPanel>
    </Grid>
</ContentDialog>
