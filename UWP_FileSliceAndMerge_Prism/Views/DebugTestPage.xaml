<Page
    x:Class="UWP_FileSliceAndMerge_Prism.Views.DebugTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_FileSliceAndMerge_Prism.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prismMvvm="using:Prism.Windows.Mvvm"
    prismMvvm:ViewModelLocator.AutoWireViewModel="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Gray">
        <Pivot x:Name="Items" Margin="0,0,0,12">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{Binding}" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem Header="Debug1">
                <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
                    <local:Debug1Page/>
                </Grid>
            </PivotItem>

            <PivotItem Header="Debug2">
                <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
                    <local:Debug2Page/>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
