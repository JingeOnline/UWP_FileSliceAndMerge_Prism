<ContentDialog
    x:Class="UWP_FileSliceAndMerge_Prism.Views.BinarySplitSettingWarningDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_FileSliceAndMerge_Prism.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Confirm Setting"
    PrimaryButtonText="Confirm"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <StackPanel>
            <TextBlock  Style="{StaticResource BodyTextStyle}">
                <Run Text="This setting will generate"/>
                <Run Text="{x:Bind ResultFileNumber}"/>
                     <Run Text="slice files after runing."/>
                <Run Text="A large number of slices generally need long time to execute."/>
            </TextBlock>
            <TextBlock Text="Notice: Considering the performance, the result preview table only show the top 5000 slices."
                       Style="{StaticResource NoticeTextStyle}"
                       Margin="{StaticResource SmallTopMargin}"/>
        </StackPanel>
    </Grid>
</ContentDialog>
